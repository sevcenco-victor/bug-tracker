pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            agent{
                docker {
                    image 'golang:trixie'
                    reuseNode true
                }
            }
            steps{
                dir('bugtracker-backend') {
                    sh 'go test -v ./...'
                }
            }
        }
    }
}